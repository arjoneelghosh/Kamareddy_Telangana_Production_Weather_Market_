// Display component for prediction results
import React from "react";
import { PredictPriceResponse } from "@/types";
import { formatCurrency } from "@/utils/formatters";
import { TrendingUp, Package, CheckCircle } from "lucide-react";

interface PredictionResultProps {
  result: PredictPriceResponse;
}

export default function PredictionResult({ result }: PredictionResultProps) {
  return (
    <div className="animate-fadeIn">
      {/* Success indicator */}
      <div className="flex items-center gap-2 mb-4">
        <CheckCircle className="w-5 h-5 text-primary-600" />
        <span className="text-sm font-medium text-primary-700">Prediction successful</span>
      </div>

      {/* Main result */}
      <div className="bg-gradient-to-br from-primary-50 to-primary-100 rounded-xl p-6 mb-4">
        <div className="flex items-start gap-4">
          <div className="p-3 bg-white/70 rounded-xl">
            <TrendingUp className="w-8 h-8 text-primary-600" />
          </div>
          <div>
            <p className="text-sm text-primary-700 font-medium mb-1">Predicted Price</p>
            <p className="text-4xl font-bold text-primary-800">
              {formatCurrency(result.predicted_price)}
            </p>
            <p className="text-xs text-primary-600 mt-1">per quintal (100 kg)</p>
          </div>
        </div>
      </div>

      {/* Details */}
      <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
        <div className="flex items-center gap-3 bg-gray-50 rounded-lg p-3">
          <Package className="w-4 h-4 text-gray-400 flex-shrink-0" />
          <div>
            <p className="text-xs text-gray-500">Commodity</p>
            <p className="text-sm font-semibold text-gray-800">{result.commodity}</p>
          </div>
        </div>

        {result.market && (
          <div className="flex items-center gap-3 bg-gray-50 rounded-lg p-3">
            <div className="w-4 h-4 text-gray-400 flex-shrink-0">🏪</div>
            <div>
              <p className="text-xs text-gray-500">Market</p>
              <p className="text-sm font-semibold text-gray-800">{result.market}</p>
            </div>
          </div>
        )}

        {result.confidence !== undefined && (
          <div className="flex items-center gap-3 bg-gray-50 rounded-lg p-3">
            <div className="w-4 h-4 text-gray-400 flex-shrink-0">📊</div>
            <div>
              <p className="text-xs text-gray-500">Confidence</p>
              <p className="text-sm font-semibold text-gray-800">
                {(result.confidence * 100).toFixed(1)}%
              </p>
            </div>
          </div>
        )}
      </div>

      <p className="mt-4 text-xs text-gray-400">
        * This prediction is generated by the AI model and is for informational purposes only.
      </p>
    </div>
  );
}
